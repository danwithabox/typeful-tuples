import { defineConfig, mergeConfig } from "vitest/config";
import { vitestConfigBase_forSpecificTsVersionWorkspace } from "../vitest-workspaces-shared.js";

export default mergeConfig(
    vitestConfigBase_forSpecificTsVersionWorkspace(`{{config_expectedTypescriptVersion}}`, async () => (await import("typescript")).default.version),
    defineConfig({
        test: {
        },
    }),
);
